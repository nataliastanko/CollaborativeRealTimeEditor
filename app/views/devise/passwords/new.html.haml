%div.page-header
  %h1 Nie pamiętasz hasła?

= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post}) do |f| 
  = devise_error_messages!
  
  %p Podaj swój adres email aby uzyskać instrukcje do odzyskania hasła
  
  %div.field
    = f.label :email, "Adres E-mail"
    = f.email_field :email
  
  %div.actions
    = f.submit "Wyślij", :class => 'btn btn-primary'