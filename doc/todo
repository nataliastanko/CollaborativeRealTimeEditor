TODO 
- działanie na wielu paragrafach 
- zintegrowac login autoryzację railsowa z aplikacją socky 
(websocket robi najpierw request do akcji w kontrolerze w mojej aplikacji (sockycontroller), powinien zwrocic usera)


rozdzielic na kanały dokumenty 1 dokument to 1 kanał, np id dokumentu to kanal

edycja ttulu doc [rzez ajax

w Editor przechowywac ktory paragraf ma focus
i w jakim moejscu

odswiezanie nowej linii
kanały
autoryzacja usera

dodac przypadek dodawania linii na poczatku dokumentu

focus na nowo dodanej linii daje edytor is null

usuwanie paragrafu

koncepcyjny algo wypisywania dokumentu

delete, shitf enter, strzałki

array = array();
    foreach (document.lines as line){
      array[line.id] = line;
    }
    
    array
    [id] = obiekt
    
    line =array.first
    line.text
    
    while (line= line[next]) {
      line.text
    }
    
    
    
    while (line = line.next)
      line.text
      
      
      kyrsor po backspace niech wraca gdzie trzeba a nie do poczatku (focus)
      jak szybko zrobic enter miedzy wyrazami, to powiela sie rozdzielenie  
